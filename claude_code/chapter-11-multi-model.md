# Claude 不是万能的：聪明人都在用多模型流水线，省钱又提速

> Claude 擅长推理但贵、本地模型便宜但笨、GPT 写文档强但代码弱——干嘛非选一个？把任务拆开，分给最合适的人干。哦不，最合适的模型。

---

前几篇都在讲 Claude Code 怎么用。但这篇要说一句你可能不太爱听的话：

**不是所有任务都值得让 Claude 做。**

Claude 擅长的是复杂推理、多文件联动、长代码重构——它在这些场景确实天下无敌。但在另一些场景——"把所有 console.log 去掉"、"生成 API 文档模板"、"翻译 10 个字段的中文名"——你让它做这些，就好像让一个架构师去扫地。

这篇讲的是怎么把几个 AI 组合起来，用**多模型流水线**——便宜模型干简单活，Claude 干重活。总成本降一半，速度还更快。

---

**本文目录：**

- 一、Claude Code 的甜区在哪里
- 二、什么时候该换模型
- 三、本地模型入局：Ollama 做简单任务
- 四、构建你的第一条多模型流水线
- 五、多模型协作的避坑指南

---

## 一、Claude Code 的甜区在哪里

### 它最擅长的事

用一句话总结：**需要"理解关系"的任务。**

什么是"理解关系"？

- 你改了数据库 Schema，需要找到所有受影响的后端接口、前端调用、测试 mock、SQL 语句——**这是关系**（schema → api → ui → test）
- 一个 Bug 出现在登录流程，但你不知道前端还是后端的问题——**这是关系**（请求链路追踪）
- Code Review 发现一处写法不规范，想知道全项目还有没有类似问题——**这是关系**（代码模式搜索）
- 重构一个状态管理逻辑，从 Redux 迁到 Zustand——**这是关系**（全局状态 → 组件 → selector → dispatch）

这些任务的共同特征：**不是改一行代码的问题，是搞清楚代码之间怎么连接的。**

### 甜区之外：它的"性价比低谷"

以下任务让 Claude 做，效果一样好——但价格是你多付的：

| 任务 | 为什么不需要 Claude | 替代方案 |
|------|-------------------|---------|
| 批量格式化代码 | 纯规则操作，0 推理 | Prettier |
| 去 console.log | 纯正则搜索替换 | Haiku / 本地模型 |
| 翻译字段名 | 单词级翻译，不需要代码理解 | Haiku |
| 生成样板代码 | 模板化，不需要决策 | 本地模型 / Snippet |
| 写单元测试框架 | 固定结构，填充数据而已 | Haiku |
| 生成 commit message | 读 diff → 总结，不需要深度推理 | Haiku / 本地模型 |
| 答简单 API 问题 | "这个函数参数是什么" | 本地模型 / grep |

### 一个决策公式

问自己三个问题：

1. **这个任务需要理解至少 3 个文件之间的关系吗？** → 是：Claude。否：往下看。
2. **这个任务有明确的、唯一的正确答案吗？** → 是：便宜模型或工具。否：Claude。
3. **这个任务的输出会被我直接部署或发布吗？** → 是：Claude Review。否：便宜模型。

这三个问题能筛掉 60% 没必要用 Claude 的任务。

---

## 二、什么时候该换模型

### 实时性要求高的场景

Claude 的推理需要时间——有时候要 10-30 秒出一段复杂代码。对开发来说还好，但如果是"用户在等"的场景——比如 IDE 里的自动补全——你等不了。

这类场景更适合用本地部署的轻量模型或者更快的云 API。

### 批量处理场景

"把这 50 个 markdown 文件里的 API 文档格式统一一下"。

逐个处理，不需要跨文件理解——每篇都是独立任务。这种"量大但不复杂"的场景是最适合用便宜模型的。

### 成本敏感的大团队

一个 20 人的团队都用 Claude Code，每人每月烧 $80。技术负责人说"我们要省点"——但你没法要求大家"少用 AI"，那等于要求大家"干活慢点"。

多模型策略在这种场景下是唯一解法：保留 Claude 用于核心任务，简单任务路由到便宜模型。质量不打折，成本降 60%。

---

## 三、本地模型入局：Ollama 做简单任务

### 本地模型的瓶颈和机会

用一个事实开头：**本地模型在复杂推理上跟 Claude 不是一个量级。**

但另一个事实：**简单任务占总交互量的 40-50%。**

你可以用 Claude Code 的 CLI 参数指定模型。如果本地跑了 Ollama：

```bash
# Claude 做复杂推理
claude "重构 src/services/order.ts 的退款逻辑"

# 本地模型做简单事
claude --model ollama/qwen2.5-coder:7b "去掉 src/ 下所有 tsx 文件里的 console.log"
```

### 本地模型的三个最佳场景

**场景一：代码格式化 / 批量替换**

> "把 src/api/ 下所有文件的导入语句按项目规范重排"

本地模型做到 90 分。Claude 做到 95 分。多花 8 倍钱换 5 分——不值。

**场景二：生成 commit message / PR 描述**

这个在前一章详细讲过了。读 diff、总结、套规范——不需要深度推理，给便宜的做。

**场景三：代码注释和文档生成**

> "给 src/utils/helpers.ts 里每个函数加 JSDoc 注释"

纯格式化的活。让本地模型出一个草稿，你再审——比从头写快得多。

### 硬件门槛参考

| 模型 | 最低显存 | 推荐显存 | 适合的任务 |
|------|---------|---------|-----------|
| Qwen2.5-Coder 7B | 6GB | 8GB | 简单格式化、注释 |
| Qwen2.5-Coder 14B | 10GB | 16GB | 中等复杂度的代码变更 |
| DeepSeek-Coder 33B | 20GB | 24GB | 接近 Sonnet 水平的推理（部分场景） |

MacBook Pro M1 Max 32GB 可以轻松跑 14B 级别的模型。普通笔记本可能只能跑 7B——够用，别期望太高。

---

## 四、构建你的第一条多模型流水线

### 不是"选一个"，是"分任务"

多模型流水线的核心逻辑：

```
用户请求 → 任务路由器 → 简单任务 → 本地模型 / Haiku
                         → 复杂任务 → Sonnet / Opus
```

你不一定需要代码级的路由器。但你可以通过**命名约定**来给自己的指令分类。

### 示例：一个典型工作日的任务分配

假设你今天要做以下 5 件事：

1. 去掉 `src/components/` 下所有 `console.log` ← **→ 本地模型**
2. 重构 `src/services/order.ts` 的退款状态机 ← **→ Claude Sonnet**
3. 写完代码后跑 14 个测试，修到全绿 ← **→ /goal + Sonnet**
4. 把 3 个英文字段名翻译成中文注释 ← **→ Haiku**
5. Review 重构之后的架构改动 ← **→ Claude Opus（贵但值）**

如果用 Claude Sonnet 做全部 5 件事：约 $4.5。
用多模型策略：约 $1.8。

每天省 $2.7。一个月 20 个工作日 = $54。一年 ≈ $600。

### 进阶：让 Claude Code 自己路由

这件事已经可以自动化了：

> "分析以下任务清单，把每个任务标上'复杂度：低/中/高'和'推荐模型：Haiku/Sonnet/Opus'，给出原因。"

然后按推荐去分配。每次花 150 个 token 做一次路由判断，省的是几百倍的 token。

### 一个完整的流水线实例

假设你做一次"为 PayService 加单元测试"的流程：

```
第 1 步：分析 PayService 源码，确定测试范围
→ 模型：Haiku（只读代码、提取函数签名和分支——纯体力活）
→ 时间：5 秒
→ 成本：~$0.01

第 2 步：生成测试代码
→ 模型：Sonnet（需要理解业务逻辑，设计测试用例）
→ 时间：15 秒
→ 成本：~$0.08

第 3 步：运行测试，修复失败
→ 模型：/goal + Sonnet（自动迭代修到全绿）
→ 时间：2 分钟
→ 成本：~$0.15

第 4 步：Review 测试覆盖率和质量
→ 模型：Opus（深度审查，一次性的——值得花这个钱）
→ 时间：10 秒
→ 成本：~$0.30

总时间：约 2.5 分钟
总成本：~$0.54
```

如果全用 Sonnet 做，成本约 $1.2。如果全用 Opus，成本轻松破 $5。

---

## 五、多模型协作的避坑指南

### 坑一：不同模型对同一份代码的理解可能不一样

本地模型的推理能力弱，你对它的指令需要更具体、更模板化。

> ❌ 对本地模型："优化这个函数的性能"

> ✅ 对本地模型："把 `users.map(user => user.name)` 里的 for 循环改成数组方法，不要动其他逻辑"

给本地模型的指令要像给新手的指令——每一步都写清楚。

### 坑二：不要在同一个对话里切换模型

Claude Code 的对话上下文是模型绑定的。你在 Claude 的对话里生成的东西，如果切到本地模型，历史上下文可能格式不兼容。

**不同模型用不同对话窗口。** 简单任务的输出复制过来当参考就行。

### 坑三：过度优化

看了这篇文章之后，你可能会想"先把所有任务归类，再逐个分配模型"。这就过头了。

**记住核心原则：** 如果任务需要"理解多个文件之间的关系" → Claude。否则 → 便宜模型或工具。就这一个判断标准。花在"该用哪个模型"上的决策时间不要超过 5 秒。

---

## 写在最后

这篇文章其实不是一个"省钱教程"。它的核心观点是：

**把 AI 当作一个工具箱，而不是一把万能钥匙。**

Claude 是把好螺丝刀，但不是锤子。你要钉钉子，找便宜模型——或者直接用 Prettier，它免费。

用多模型策略的时候，你是架构师——不只是写代码的架构师，也是"把工作分给谁做"的架构师。这个角色跟传统的程序员相比，多了一层决策权。用好它。

---

*Claude Code 实战系列 · 多模型协同*
*适用对象：对 API 账单有感觉的开发者 / 想引入 AI 但担心成本的团队 / 已经跑了本地模型的硬件玩家*
*本系列更多文章可在公众号合集「Claude Code 实战」中查看*
