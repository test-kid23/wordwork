# CSDN 技术文章排版与写作规范

> 适用于 CSDN 技术博客的 Markdown 写作格式。
> 核心原则：**专业技术分享，重命令使用与问题分析方法论，保持内容严谨与可复现性。**

---

## 一、CSDN 文章的核心定位

跟微信公众号、小红书完全不同，CSDN 用户的阅读预期是：

| 维度 | CSDN | 微信公众号 |
|------|------|-----------|
| **读者动机** | 搜索技术问题 → 找解决方案 | 订阅号流 → 刷到感兴趣的内容 |
| **内容期待** | 能直接用的命令、能复现的分析过程 | 观点、故事、认知升级 |
| **标题逻辑** | 技术关键词锚定搜索流量 | 痛点/好奇心驱动点击 |
| **风格** | 专业、严谨、可操作 | 口语化、有情绪、有立场 |
| **代码/命令** | 核心内容，必须完整可复制 | 辅助说明，点到为止 |

**一句话：CSDN 读者是带着问题来的，他要的是答案，不是故事。**

---

## 二、标题规范

### 原则

- **技术关键词前置**：把用户真会搜的技术词放在标题前段（如 `iostat`、`NVMe`、`IO 瓶颈`、`Linux 磁盘排查`）
- **直给主题，拒绝钩子**：不用"你肯定遇到过""震惊""一个案例教会你"这类好奇心驱动写法
- **可以加数字限定范围，但别过度承诺**：比如"4 个常见误区"比"彻底解决所有磁盘问题"好

### 公式

```
[核心技术名词] + [问题场景/分析对象] + [方法/角度限定词（可选）]
```

### 示例

| ✅ CSDN 风格 | ❌ 公众号风格（不适合 CSDN） |
|-------------|---------------------------|
| `iostat 磁盘 IO 瓶颈排查：%util、await、aqu-sz 指标深度解析` | `4 块 NVMe 盘都被打满了，排查结果让我后背发凉` |
| `Linux 磁盘 IO 性能分析工具链：iostat、iotop、blktrace 实战` | `凌晨三点服务器炸了，我用三行命令救了回来` |
| `NVMe 磁盘高负载排查方案：从 iostat 到 fio 压测全流程` | `90% 的运维不知道，磁盘打满 iowait 居然为 0` |
| `blktrace 深度使用指南：定位 Linux 块设备 IO 延迟瓶颈` | `别再被 %util 骗了！NVMe 时代的磁盘排查新姿势` |

### 注意事项

- 总字数控制在 **15-30 字**
- 如果涉及版本/平台限定，标题里标出来（如 `Ubuntu 22.04`、`NVMe Gen4`）
- 系列文章标题格式统一，比如 `csdn-XX-主题.md`

---

## 三、正文结构

### 3.1 推荐骨架

CSDN 文章不需要"讲故事的开场"，但可以保留 **问题场景 → 分析过程 → 解决方案 → 总结** 的骨架。

```markdown
## 一、问题背景
（什么场景下遇到的这个问题？什么现象？环境是什么？）

## 二、核心概念/工具说明
（涉及的命令、字段、指标解释。保证读者能跟上后面的分析。）

## 三、排查过程
（按步骤写，每一步：用了什么命令 → 输出了什么 → 怎么解读的 → 得到了什么结论）

## 四、解决方案 / 优化措施
（具体做了什么？命令是什么？配置怎么改？效果数据对比）

## 五、补充：常见误区 / 注意事项 / 扩展工具
（可选。挖深一点，或者排雷。）

## 六、总结 / 速查表
（一张表或几句话，方便读者快速回顾。不是升华，是实用总结。）
```

### 3.2 结构原则

1. **层次清晰但不过度嵌套** —— 三级标题（`###`）够了，尽量不出现四级标题
2. **每个章节解决一个问题** —— 别在一个章节里塞多个不相关的点
3. **代码块前后必须有说明** —— 读者要知道"为什么跑这个命令"和"怎么读输出"

---

## 四、代码与命令规范

CSDN 文章的核心价值很大程度在代码块。写不好代码块，文章废一半。

### 4.1 命令必须完整可复制

```bash
# ✅ 好的写法——读者复制粘贴就能跑
iostat -xmt 1 5

# ❌ 坏的写法——只有命令没有参数说明，或者参数是假的
iostat  # 然后看输出
```

### 4.2 输出/结果必须给出关键部分的解读

不要贴一整段日志然后说"如上所示"。关键行加注释或紧接着解读：

````markdown
```bash
iostat -xmt 1
```
输出中重点关注这几列：

| 字段 | 当前值 | 含义 | 是否异常 |
|------|--------|------|----------|
| `%util` | 100% | 磁盘繁忙占比 | ⚠️ 持续饱和 |
| `r_await` | 0.09ms | 读延迟 | 暂时正常 |
| `aqu-sz` | 2.7 | IO 队列长度 | ⚠️ 持续排队 |
````

### 4.3 配置文件修改要给出"改前 vs 改后"

```markdown
**改前（/etc/sysctl.conf）：**
```
# vm.dirty_ratio = 20
```

**改后：**
```
vm.dirty_ratio = 5
vm.dirty_background_ratio = 2
```

**为什么这么改：** 当前服务器内存 128G，20% 是 25.6G 脏页上限，IO 压力大时一次性刷盘会形成写入风暴。缩小到 5% 让刷盘更分散。
```

### 4.4 代码块语言标注

每个代码块必须标注语言类型：
- `bash` → shell 命令
- `python` → Python 代码
- `yaml` / `json` / `ini` → 配置文件
- `text` / `plaintext` → 无语法高亮的纯文本输出

---

## 五、表格使用规范

表格在 CSDN 里是很好的信息组织方式，但需要遵守：

1. **表头必须清晰** —— 三列以上必须给每列一个一眼能看懂的表头
2. **不要用表格讲长句** —— 表格单元格内容控制在 20 字以内，超出改用列表
3. **表格前后留空行和文字过渡** —— 别连续砸 3 个表格在读者脸上

### 适合用表格的场景

- 命令/参数对照表
- 指标字段含义速查
- 方案对比（方案 A vs 方案 B）
- 排查步骤速查

### 不适合用表格的场景

- 长篇分析论述
- 代码示例
- 个人观点/经验总结

---

## 六、语气与表达规范

### 6.1 CSDN 适用的语气

| ✅ 可以用 | ❌ 不要用 |
|----------|----------|
| 客观陈述（"经测试发现"） | 情绪化感叹（"我直接裂开"） |
| 第一人称经验（"我在排查中发现"） | 过度讲故事（"那天凌晨两点…"） |
| 直接判断（"这个参数不建议调"） | 端水表述（"各有优劣，根据场景选择"——除非真的各有优劣且有对比数据） |
| 技术用语准确 | 公众号式口语化转折词（"你猜怎么着""这就很操蛋了"） |

### 6.2 仍须遵守的"去 AI 味"规则

以下规则**全平台通用**，CSDN 也不例外：

- **禁止"首先…其次…最后…"的排比结构** —— 用"第一个问题""另外""还有"替代
- **禁止教科书式总结** —— 结尾不要"综上所述""本质上""核心在于"这类升华金句，直接给结论或速查表
- **禁止虚假客观套话** —— "根据官方文档…最佳实践是…"改成直接陈述，如果引用了官方文档就明确给链接或 man 页面引用
- **可以留白** —— 不需要每个概念都解释到底，非核心细节说"感兴趣的可以搜 XXX"没问题

### 6.3 CSDN 专属豁免（和公众号不一样的地方）

以下在公众号文章里要严格规避的写法，**在 CSDN 里可以适度使用**：

| 写法 | 公众号 | CSDN |
|------|--------|------|
| "让我们来看…""大家可以看到…" | ❌ 教学腔禁词 | ✅ 教学文章可以适度出现 |
| 三级嵌套标题 + 列表结构 | ❌ 过度结构化 | ✅ 技术文章需要层次 |
| 连续表格/代码块 | ❌ 格式密度限制 | ✅ 技术文章天然需要 |
| 纯技术叙述无个人经历 | ❌ 零个人经历 | ✅ 不是每篇都需要个人故事 |

**底线：** 可以结构化，但不能读起来像 AI 写的教科书。关键区别在于——你有没有给出"别人不会这样告诉你"的具体见解。

---

## 七、排版格式（Markdown）

### 7.1 标题层级

```markdown
# 一级标题 → 文章标题（只有一个）
## 二级标题 → 大章节
### 三级标题 → 小节
```

避免四级标题（`####`），用加粗列表项代替。

### 7.2 分隔线

章节之间用 `---` 分隔。不要用花哨符号。

### 7.3 加粗与强调

- **关键结论**、**重要数值**、**命令名称** 加粗
- 不要整段加粗，失去强调效果
- 行内代码用反引号 `` ` ``，不要加粗反引号

### 7.4 列表

- 步骤型内容用有序列表 `1. 2. 3.`
- 并列要点用无序列表 `-`
- 列表项超过 3 行换段落叙述

---

## 八、写完后的自查清单

- [ ] 标题是否包含核心技术关键词？（读者能搜到吗？）
- [ ] 代码块是否完整可复制？（有没有漏参数？）
- [ ] 代码块前后有没有说明？（为什么跑、怎么读输出？）
- [ ] 有没有出现「首先…其次…最后」？→ 打散
- [ ] 结尾是不是升华金句（"本质上""核心在于"）？→ 换成大白话或速查表
- [ ] 表格和代码块之间有没有文字过渡？→ 别连续砸
- [ ] 所有外部引用的命令/参数是否有明确来源？→ man 页、官方文档、实测
- [ ] 文章的结论能不能被读者复现？→ 命令相同、环境相近、结果可预期

---

## 九、文件命名规范

```
csdn-{序号}-{简短英文主题}.md
```

示例：
- `csdn-01-requests-ai-practice.md`
- `csdn-02-playwright-ai-practice.md`
- `csdn-06-disk-io-bottleneck-analysis.md`

系列子目录下的文件保持一致性，如：
- `selenium_webui/csdn-selenium-01-env-setup.md`
- `appium_mobile/csdn-appium-02-minimum-chain.md`
