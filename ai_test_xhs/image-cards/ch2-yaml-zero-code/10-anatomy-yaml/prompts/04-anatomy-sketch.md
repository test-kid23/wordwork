# 10-04-anatomy-sketch

## Prompt

A hand-drawn educational infographic in sketch-notes style, vertical 3:4 aspect ratio, warm cream paper-texture background. Macaron pastel palette.

Content fills the image naturally with generous proportions.

Section title at top: hand-lettered bold Chinese "解剖一个真实用例" with coral red squiggly underline.

Subtitle: hand-lettered Chinese "创建新用户" in medium size with a small magnifying glass doodle.

Main content area — a large hand-drawn rounded card (cream slightly darker than bg), like annotating a document:

Four labeled sections with hand-drawn brackets and arrows:

Section 1 — peach tag "名称":
- Hand-lettered Chinese "描述这个用例做什么"
- Arrow pointing to example: "创建新用户"

Section 2 — mint tag "请求":
- Hand-lettered Chinese "POST 到指定地址"
- Arrow pointing to: "方法 POST / URL /users"
- Small note "参数用花括号引用全局变量"

Section 3 — lavender tag "提取":
- Hand-lettered Chinese "从响应里抠出用户ID"
- Arrow pointing to: "user_id 存起来"
- Small note "给后面的用例用"

Section 4 — blue tag "断言":
- Hand-lettered Chinese "验两样：状态码和用户名"
- Arrow pointing to: "状态码是 201 / 用户名一致"

Bottom insight area: hand-drawn lightbulb with coral accent text split into short lines:
Line 1: "没有一句代码"
Line 2: "但该做的事都做了"

Doodle decorations: tiny stars, wavy connecting lines. Slight wobble on everything. No computer fonts.

@Testkid watermark hand-lettered at bottom-right, positioned well above the very bottom edge (roughly 8-10% up from bottom).
